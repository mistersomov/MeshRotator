#ifndef MESH_ROTATOR_MATERIAL_HPP
#define MESH_ROTATOR_MATERIAL_HPP

namespace ndk_helper {
    namespace core {
        class Material {
        protected:
            virtual ~Material() = default;
        };
    }
}

#endif //MESH_ROTATOR_MATERIAL_HPP
